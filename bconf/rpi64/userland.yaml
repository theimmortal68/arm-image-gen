---
mmdebstrap:
  # Install Raspberry Pi userland & camera utilities AFTER repo is added by firmware.yaml
  packages:
    - libraspberrypi0
    - libraspberrypi-bin
    - libcamera0
    - libcamera-tool
    - libcamera-dev
    - rpicam-apps   # uncomment if you ship the newer camera apps
    - raspinfo
    - raspi-config
    - raspberry-sys-mods
