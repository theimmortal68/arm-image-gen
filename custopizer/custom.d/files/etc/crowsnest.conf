# Minimal Crowsnest config using camera-streamer
[global]
log_level = info

[streamer]
type = camera-streamer
device = auto
# Optional tweaks:
# resolution = 1280x720
# framerate  = 30
# bitrate    = 4000k
